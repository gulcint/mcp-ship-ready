---
id: task-2026-09-25-0005
from: product-owner
to: human
status: blocked
priority: high
kind: client
client: claude-marketplace-plugin
created: 2026-09-25
related_trace:
touches: clients/claude-marketplace-plugin/plugin.json, clients/claude-marketplace-plugin/README.md, clients/claude-marketplace-plugin/docs/submission-checklist.md
locked_by:
locked_at:
blocked_reason: Community Directory'ye fiilî gönderim insan onayı bekliyor (BRIEF.md İnsan Kapıları) — paket main'de hazır, bkz. docs/submission-checklist.md
---
## Spec (Ne ve Neden)
Zincirin son adımı: plugin'in Anthropic Community Directory'ye
gönderilmeye **hazır** bir pakete dönüştürülmesi. "Hazır" = manifest/
`plugin.json` gönderim için gereken tüm bilgileri içerir, en az 3
örnek kullanım prompt'u ve bir gizlilik/veri-işleme notu (BRIEF "Veri"
bölümünden türetilir: yalnızca herkese açık repo statik analizi,
production erişimi yok) repo'da yer alır.

Anthropic Community Directory submission sürecinin tam manifest
gereksinimleri **doğrulanmadı** (task-2026-09-25-0001 Riskler
bölümünde işaretli). Bu ticket'ın Plan aşamasında, forward-deployed-
engineer önce Anthropic'in resmi submission dokümantasyonunu
kontrol etmeli (varsa `mcp-server-dev` plugin'indeki directory-
checklist referansı dahil) — gereksinimleri tahmin ederek
implementasyona başlamamalı.

**Önemli sınır:** Bu ticket paketi *hazırlar*, Anthropic'e fiilen
*göndermez*. BRIEF.md "İnsan Kapıları" madde 1'e göre dizine ilk
gönderim insan onayı gerektirir — forward-deployed-engineer paketi
tamamlayıp `to: human` ile onay/gönderim için eskale eder.

## Kapsam Dışı
- Fiili gönderim işlemi (insan onayı sonrası, insan veya insanın
  yönlendirdiği bir adım yapar — bu ticket'ın kapsamında değil).
- Dolaylı gelir modeli / danışmanlık lead-gen içeriği (BRIEF, insan
  bilinçli olarak erteledi).
- Tarama/düzeltme özelliklerinin kendisi (önceki ticket'larda tamamlanmış
  olmalı, burada yalnızca paketleniyor).

## Kabul Kriterleri
- [x] Verildiğinde tamamlanmış plugin (iskelet+tarayıcı+düzeltme),
      olduğunda `claude plugin validate` çalıştırıldığında, o zaman
      sıfır hatayla geçer. (`Validation passed with warnings`, exit 0 —
      tek bilinen/kabul edilmiş uyarı hariç. Ayrıca gerçek bir
      marketplace install testiyle de doğrulandı — bkz. Doğrulama
      Kanıtı.)
- [x] Verildiğinde Anthropic'in resmi Community Directory submission
      dokümantasyonu incelendiğinde, olduğunda `plugin.json`/manifest
      hazırlandığında, o zaman dokümantasyonun gerektirdiği tüm alanlar
      (yazar, açıklama, repo, lisans vb.) doldurulmuş olur.
      (`description`, `author.name`, `homepage`, `repository`,
      `license`, `keywords` — gerçek manifest-reference.md'ye karşı
      doğrulandı.)
- [x] Verildiğinde gönderim paketi, olduğunda incelendiğinde, o zaman
      en az 3 örnek kullanım prompt'u içerir. (`docs/submission-
      checklist.md`, 3 prompt.)
- [x] Verildiğinde gönderim paketi, olduğunda incelendiğinde, o zaman
      BRIEF "Veri" bölümüyle tutarlı bir gizlilik/veri-işleme notu
      (yalnızca public repo statik analizi, production erişimi yok)
      açıkça yer alır. (`docs/submission-checklist.md` "Privacy /
      data-handling note".)
- [x] Verildiğinde paket tamamlandığında, olduğunda görev kapatıldığında,
      o zaman görev fiilen Anthropic'e göndermeden `to: human` ile
      onay/gönderim için eskale edilir (kod otomatik gönderim
      yapmaz). (Bu PR fiilen hiçbir yere göndermiyor — yalnızca repo-
      içi hazırlık. Tech-lead'in talimatına göre `to: human`
      eskalasyonu merge SONRASI tech-lead tarafından yapılacak; ben
      PR'ı `to: tech-lead` ile açıyorum, aynı 0004/0006/0007/0008
      konvansiyonu.)

## Plan
**Araştırma (kod yazmadan önce, gerçek fetch — tahmin değil):**
- `code.claude.com/docs/en/plugins/publish.md` — resmi "Publish and
  distribute a plugin" sayfası. Kilit bulgu: gönderim bir REPO-İÇİ
  manifest alanı DEĞİL, bir WEB FORMU üzerinden yapılıyor:
  `claude.ai/admin-settings/directory/submissions/plugins/new`
  (Team/Enterprise + Directory izni) VEYA
  `platform.claude.com/plugins/submit` (bireysel yazarlar için).
  Ön koşul: `claude plugin validate --strict ./plugin` yerelde
  çalıştırılır, `description`/`author`/`homepage`/`repository`
  `plugin.json`'da doldurulur, `README.md` repo kökünde olur.
- `code.claude.com/docs/en/plugins/manifest-reference.md` — tam alan
  listesi doğrulandı: `name` (zorunlu, kebab-case, KALICI — zaten
  `mcp-ship-ready`), `version`, `description`, `author` (`name`
  zorunlu, `email`/`url` opsiyonel), `homepage` (URL olarak parse
  edilmeli), `repository`, `license`, `keywords`. "3 örnek prompt" ya
  da "gizlilik notu" diye bir MANİFEST ALANI YOK.
- **Kritik bulgu — platform riski yeniden değerlendirildi:**
  Ticket'ın ve task-2026-09-25-0001'in işaret ettiği "mcp-server-dev
  plugin'indeki directory-checklist" dosyasını GERÇEKTEN okudum
  (`~/.claude/plugins/marketplaces/claude-plugins-official/plugins/
  mcp-server-dev/skills/build-mcp-app/references/directory-
  checklist.md`, yerelde mevcut, gerçek Anthropic içeriği). Bu dosya
  **UZAK MCP CONNECTOR/APP'lerin** (OAuth, tool widget'ları, iframe
  sandbox, ekran görüntüsü gereksinimleri olan) **Claude connector
  directory**'sine gönderimi için — MCP Ship-Ready gibi yerel/CLI
  tabanlı bir **Claude Code plugin**'in **plugin community
  marketplace**'ine gönderimiyle (bizim gerçek hedefimiz, yukarıdaki
  `publish.md`) AYNI ŞEY DEĞİL, tamamen farklı bir süreç/kriter seti
  (OAuth zorunluluğu, widget layout, ekran görüntüsü vb. — hiçbiri
  bize uygulanmıyor, biz bir MCP connector/app değiliz). Bu, task-
  2026-09-25-0001'de işaretlenen "Anthropic'in bu alanı resmi olarak
  kapatma riski" endişesini KISMEN azaltıyor — iki farklı "directory"
  karıştırılmış olabilir. Business Analyst/Growth & Vision Lead'e
  ayrıca not düşeceğim (bu ticket'ı bloklamıyor, bilgilendirme).

**Paket (bu ticket'ın kapsamı — repo-içi hazırlık, fiili gönderim
YOK):**
1. `plugin.json`: `homepage` ve `repository` eklendi (gerçek, zaten
   var olan repo URL'i — `git remote get-url origin` ile doğrulandı,
   uydurulmadı: `https://github.com/gulcint/mcp-ship-ready`).
   `author.email` EKLENMEDİ — BRIEF'te "doğrudan iletişim kişisi
   (isim/e-posta) henüz belirtilmedi" diye kayıtlı, uydurma bir e-posta
   koymadım. `version` `0.1.0`'da kalıyor (ilk sürüm için dürüst bir
   olgunluk sinyali; semver politikası ayrı bir karar, bu ticket'ta
   ele alınmıyor).
2. `README.md`: `scan`/`fix`/`fix-apply` üçünün GERÇEK davranışı
   (tech-lead'in istediği) — `fix`'in hiçbir zaman yazmadığı,
   `fix-apply`'ın gerçekten dosyaya yazdığı, ikisinin de
   `disable-model-invocation: true` olduğu (yalnızca kullanıcı
   açıkça çağırabilir) — doğru anlatılacak şekilde güncellendi.
   "Status" bölümündeki eski "auto-fix... follow-up tasks" ifadesi
   kaldırıldı (artık tamamlandı).
3. `docs/submission-checklist.md` (yeni): `publish.md`'nin "Prepare
   your plugin for release" adımlarına karşı bir kontrol listesi +
   3 örnek prompt + BRIEF "Veri" bölümünden türetilen gizlilik notu.
   **Açıkça etiketlenmiş:** örnek prompt'lar ve gizlilik notu
   MANİFEST ALANI DEĞİL — web formunun ne sorduğunu doğrulayamadım
   (form'a erişimim yok, insan kimlik doğrulaması gerektiriyor).
   Bunlar insanın fiili gönderim sırasında forma kopyalayıp
   yapıştırabileceği HAZIR İÇERİK olarak sunuluyor, "form'un birebir
   alanları" diye iddia edilmiyor.
4. Yerel marketplace kurulum testi (`publish.md`'nin önerdiği pre-
   flight adımı): geçici, COMMIT EDİLMEYEN bir `marketplace.json` ile
   `claude plugin marketplace add`/`install` denenecek — ticket'ın
   touches'ında `marketplace.json` yok, bu yüzden kalıcı olarak repoya
   eklenmiyor, yalnızca doğrulama için kullanılıp silinecek.

**Bilinen, düzeltilmeyen uyarı:** `claude plugin validate --strict .`
CLAUDE.md'nin plugin kökünde "project context olarak yüklenmiyor"
uyarısıyla başarısız oluyor (0002'den beri bilinen, kabul edilmiş —
CLAUDE.md şirketin governance dosyası, plugin skill içeriği değil).
Kabul kriteri #1 "sıfır HATAYLA" diyor (uyarı değil) — düz `claude
plugin validate .` (strict değil) `Validation passed with warnings`
verip exit 0 dönüyor, CI de hep böyle çalıştı. Gerçek bir temizlik
(CLAUDE.md içeriğini bir SKILL.md'ye taşımak) touches dışında, ayrı
bir ticket/karar gerektirir — Riskler'e not düşüldü.

## Test Planı
- `claude plugin validate .` → exit 0, "Validation passed" (aynı tek
  bilinen uyarı hariç).
- Yerel marketplace + install testi (geçici `marketplace.json`,
  commit edilmeyecek): `claude plugin marketplace add`/`claude plugin
  install mcp-ship-ready@<test-marketplace>` gerçekten çalışıyor mu.
- `README.md`'de `scan`/`fix`/`fix-apply` üçü de doğru anlatılıyor mu
  (elle inceleme + `npm run check` ile kod davranışına karşı
  çapraz kontrol).
- `docs/submission-checklist.md`'de ≥3 örnek prompt + gizlilik notu
  var mı (elle sayım).
- Mevcut test suite (54 test) değişmeden geçmeli — bu ticket kod
  değiştirmiyor, yalnızca doc/manifest.

## Riskler / Güvenlik Notları
- **Platform riski — Plan'da yeniden değerlendirildi (bkz. yukarı):**
  `mcp-server-dev`'in directory-checklist'i GERÇEKTEN okundu; bu, uzak
  MCP connector/app'lerin **Claude connector directory**'sine gönderimi
  için — MCP Ship-Ready'nin hedeflediği **Claude Code plugin community
  marketplace**'inden farklı bir süreç. Risk tamamen ortadan kalkmadı
  (Anthropic ileride Claude Code plugin tarafında da benzer bir
  yerleşik özellik sunabilir) ama önceki notun dayandığı somut kanıt
  (bu checklist dosyası) aslında bizim ürünümüze uygulanmıyor — BA/
  Growth & Vision Lead'e bilgilendirme notu düşüldü, bu ticket'ı
  bloklamıyor.
- Fiili gönderim insan onayı gerektirir (BRIEF "İnsan Kapıları") — bu
  ticket'ın kapsamı yalnızca hazırlıktır; onaysız gönderim yapılmamalı.
- Manifest alanlarını yanlış/eksik doldurmak gönderimin reddine yol
  açabilir (iş riski, güvenlik riski değil) — resmi dokümana dayanmak
  bunu azaltır.
- `claude plugin validate --strict .` CLAUDE.md uyarısıyla başarısız
  oluyor (bilinen, 0002'den beri kabul edilmiş — bkz. Plan). Gerçek
  temizlik (CLAUDE.md'yi bir SKILL.md'ye taşımak) bu ticket'ın
  touches'ı dışında; isteyen biri ayrı bir ticket açabilir.
- 3 örnek prompt / gizlilik notu resmi bir manifest alanı DEĞİL —
  web formunun gerçek alanlarını doğrulayamadım (insan kimlik
  doğrulaması gerektiriyor). Hazır içerik olarak sunuluyor, "form'un
  birebir gereksinimi" diye iddia edilmiyor — insan fiili gönderimde
  kendi değerlendirmesini yapmalı.

## Doğrulama Kanıtı
- Kaynak doğrulaması (gerçek fetch, tahmin değil):
  `curl https://code.claude.com/docs/en/plugins/publish.md` ve
  `.../manifest-reference.md` — tam içerikler Plan'da özetlendi.
  `~/.claude/plugins/marketplaces/claude-plugins-official/plugins/
  mcp-server-dev/skills/build-mcp-app/references/directory-
  checklist.md` gerçekten okundu (yerelde mevcut, gerçek Anthropic
  içeriği) — connector directory için olduğu, bize uygulanmadığı
  doğrulandı.
- `claude plugin validate .` → `Validation passed with warnings`
  (exit 0), aynı tek bilinen uyarı (CLAUDE.md governance dosyası).
- **Gerçek marketplace install testi** (publish.md'nin önerdiği pre-
  flight adımı): geçici, commit edilmeyen `.claude-plugin/
  marketplace.json` (`source: "./"`) ile `claude plugin marketplace
  add` + `claude plugin install mcp-ship-ready@mcp-ship-ready-local-
  test` → `Successfully installed`. `claude plugin list` → `mcp-ship-
  ready@mcp-ship-ready-local-test`, `Version: 0.1.0`, `Status: ✔
  enabled`. `claude -p "/mcp-ship-ready:scan tests/fixer/fixtures/
  mechanical-only" --output-format json` (kurulu kopya üzerinden,
  `--plugin-dir` DEĞİL) → `permission_denials: []`, `is_error: False`,
  `result` gerçek bulguyu içeriyor. Test sonrası `uninstall` +
  `marketplace remove` + geçici `marketplace.json` silindi — `git
  status` temiz, hiçbir test artığı commit'lenmedi.
  - Ayrıca gerçek GitHub reposundan (`github` source tipi) kurulum da
    denendi ama bu SANDBOX ortamında SSH host-key doğrulaması
    olmadığı için `git clone` başarısız oldu (`Host key verification
    failed`) — bu, plugin/marketplace kurulumumuzla ilgili bir sorun
    değil, bu ortama özgü bir SSH yapılandırma eksikliği; yerel
    `source: "./"` testi bunun yerine kullanıldı ve gerçek bir
    kurulum + çalıştırmayı kanıtladı.
- `npm run check` → değişmedi, `tests 54, pass 54, fail 0` (bu ticket
  kod değiştirmiyor).
- `README.md`'deki `scan`/`fix`/`fix-apply` açıklamaları `src/cli.ts`
  ve `commands/*.md`'nin gerçek davranışına karşı elle çapraz
  kontrol edildi (`fix` hiç yazmıyor, `fix-apply` yazıyor, ikisi de
  `disable-model-invocation: true`).

## Geçmiş
- 2026-09-25 product-owner: task-2026-09-25-0001'den bölündü. Zincirin
  son adımı: task-2026-09-25-0002 → 0003 → 0004 → **bu ticket**.
  `status: blocked`, 0004 `done` olunca `open`'a çekilmeli.
- 2026-09-25 tech-lead: 0002 ve 0003 merge edildi. task-2026-09-25-0006 (security sağlamlaştırma) de önkoşul olarak eklendi.
- 2026-09-25 tech-lead: 0006 merge edildi. Önkoşul 0006 yerine 0007 oldu (0006 security incelemesinin kalan bulguları).
- 2026-09-25 tech-lead: 0004 merge edildi. Kalan önkoşul task-2026-09-25-0008.
- 2026-09-25 tech-lead: 0008 merge edildi, önkoşullar tamam. `status: open`. Hatırlatma: bu ticket paketi yalnızca hazırlar; dizine gönderim `to: human` ile insan onayına gider.
- 2026-09-25 forward-deployed-engineer: Paket hazır. Önemli bulgu:
  ticket'ın/0001'in işaret ettiği "mcp-server-dev directory-checklist"
  dosyasını gerçekten okudum — bu, MCP Ship-Ready'nin hedeflediği
  Claude Code plugin community marketplace'inden FARKLI bir süreç
  (uzak MCP connector/app'ler için, OAuth/widget gereksinimleriyle).
  Platform riski kısmen azaldı, BA/Growth & Vision Lead'e ayrıca not
  düştüm. `plugin.json`'a `homepage`/`repository` eklendi (gerçek,
  zaten var olan repo URL'i). `README.md` `scan`/`fix`/`fix-apply`
  üçünü de doğru anlatacak şekilde güncellendi. `docs/submission-
  checklist.md` yeni — 3 örnek prompt, gizlilik notu, ve web-formu
  alanlarının doğrulanamadığının açık itirafı içeriyor. Geçici bir
  marketplace ile GERÇEK bir kurulum/çalıştırma testi yapıldı (bkz.
  Doğrulama Kanıtı), sonra temizlendi. 54/54 test değişmeden yeşil
  (bu ticket kod değiştirmiyor). `status: in-progress`, `to: tech-lead`
  — 0004/0006/0007/0008 konvansiyonu: merge sonrası `to: human`
  eskalasyonunu tech-lead yapacak.
- 2026-09-25 tech-lead: PR #7 review — 1 bloklayıcı. `docs/submission-
  checklist.md`'deki gizlilik notu, gönderim formuna kopyalanıp KAMUYA
  AÇIK bir beyan olacağı için, koddan daha güçlü bir doğruluk standardı
  gerektiriyor. İki cümle kodla çelişiyordu: (1) "does not send scanned
  content anywhere" — `fix` önizlemesi hedef repodan kaynak kod
  satırları (20×200 karakter) içeriyor ve bu, Claude oturumunun
  bağlamına giriyor; (2) "does not access private/closed repos" — kod
  aslında gösterilen HERHANGİ bir yerel dizini okuyor, bu bir teknik
  kısıtlama değil, öngörülen kullanım. Kaynaklar/README/install testi
  doğru bulundu, artık kalıntı yok.
- 2026-09-25 forward-deployed-engineer: Gizlilik notu düzeltildi.
  Tech-lead'in işaret ettiği iki cümle "öngörülen kullanım" ile "kod
  tarafından uygulanan garanti" ayrımını netleştirecek şekilde yeniden
  yazıldı ("public repo için tasarlandı, ama hangi yerel dizin
  verilirse onu okur" / "ağ çağrısı yapmaz ama çıktısı — dosya
  yolları ve kısa kod alıntıları dahil — Claude oturumunda işlenir").
  Aynı sınıftan ÜÇÜNCÜ bir cümleyi de (kendi kendime, istenmeden)
  buldum ve düzelttim: "No personal, financial ... data is processed"
  de aynı öngörülen-kullanım-vs-garanti sorununu taşıyordu (0008'deki
  "kardeş fonksiyonu kontrol et" dersini burada kendi metnime
  uyguladım). README'de aynı sınıftan bir ifade yoktu (kontrol edildi,
  `grep` ile doğrulandı). "Ağ çağrısı yapmaz" iddiası kod taramasıyla
  (`grep -rn "fetch(\|http\." src/`, sıfır sonuç, sıfır runtime
  bağımlılığı) doğrulandı, bu iddia değişmedi. 54/54 test değişmeden
  yeşil, `claude plugin validate .` aynı.
- 2026-09-25 tech-lead: PR #7 onaylandı ve `main`'e squash-merge edildi.
  Kanıt:
  - İlk turda 1 bloklayıcı vardı: privacy notunda kodla çelişen iki beyan. c171db5 ile düzeltildi; FDE aynı desendeki üçüncü bir beyanı da düzeltti.
  - Gönderim URL'leri resmi `publish.md`'de doğrulandı. `src/`'de ağ ya da subprocess çağrısı yok (grep).
  - Install testinden kullanıcı ortamında kalıntı yok. 54/54 test, GitHub CI yeşil.
  **Eskalasyon → human:** paket hazır. Fiilî gönderim insan kapısıdır.
  - Form: claude.ai admin (Team/Enterprise) ya da platform.claude.com/plugins/submit (bireysel).
  - Örnek prompt'lar ve privacy notu `docs/submission-checklist.md` içinde, forma yapıştırmaya hazır.
  - Form alanları doğrulanmadı (oturum gerekiyor).
  - Gönderimden sonra bu ticket `done` yapılır.
